<%= form_for(@facultyprofile) do |f| %>
  <% if @facultyprofile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facultyprofile.errors.count, "error") %> prohibited this facultyprofile from being saved:</h2>

      <ul>
      <% @facultyprofile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dept %><br />
    <%= f.text_field :dept %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
